import type { Contact, Interaction } from '../types';
export default function InteractionTimeline({ interactions, contacts }: { interactions: Interaction[]; contacts: Contact[] }) { return <div className="space-y-3">{interactions.sort((a,b)=>b.interactionDate.localeCompare(a.interactionDate)).map(i => <div key={i.id} className="border-l-4 border-pine bg-slate-50 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><strong>{i.interactionType}</strong><span className="text-sm text-slate-500">{i.interactionDate}</span></div><p className="text-sm text-slate-600">{contacts.find(c=>c.id===i.contactId)?.name || 'Firm activity'}</p><p className="mt-2 text-sm">{i.summary}</p>{i.meetingNotes && <p className="mt-2 text-sm text-slate-600">Notes: {i.meetingNotes}</p>}{i.nextAction && <p className="mt-2 text-sm font-semibold text-pine">Next: {i.nextAction} {i.nextActionDate}</p>}</div>)}</div>; }
