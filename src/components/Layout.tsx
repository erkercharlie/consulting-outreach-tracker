import type { ReactNode } from 'react';
import { BarChart3, Building2, CalendarDays, ClipboardList, Home, PlusCircle, Settings, Users, MessageSquare } from 'lucide-react';
export type View = 'dashboard' | 'add' | 'firms' | 'contacts' | 'calendar' | 'outreach' | 'followups' | 'analytics' | 'settings';
const nav = [ ['dashboard', Home, 'Dashboard'], ['add', PlusCircle, 'Add New'], ['firms', Building2, 'Firms'], ['contacts', Users, 'Contacts'], ['calendar', CalendarDays, 'Calendar'], ['outreach', MessageSquare, 'Outreach Log'], ['followups', ClipboardList, 'Follow-Ups'], ['analytics', BarChart3, 'Analytics'], ['settings', Settings, 'Settings'] ] as const;
export default function Layout({ active, setActive, children }: { active: View; setActive: (v: View) => void; children: ReactNode }) { return <div className="min-h-screen lg:flex"><aside className="border-b border-slate-200 bg-white lg:w-72 lg:border-b-0 lg:border-r"><div className="p-5"><h1 className="text-xl font-bold text-ink">Consulting Outreach Tracker</h1><p className="mt-1 text-sm text-slate-500">Firm-first recruiting CRM</p></div><nav className="flex gap-2 overflow-x-auto px-4 pb-4 lg:block lg:space-y-1">{nav.map(([id, Icon, label]) => <button key={id} onClick={() => setActive(id)} className={`flex min-w-max items-center gap-3 rounded-md px-3 py-2 text-sm font-semibold ${active===id ? 'bg-pine text-white' : 'text-slate-600 hover:bg-slate-100'}`}><Icon size={18}/>{label}</button>)}</nav></aside><main className="flex-1 p-4 lg:p-7">{children}</main></div>; }
