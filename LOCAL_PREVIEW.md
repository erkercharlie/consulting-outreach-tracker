# Local Preview Workflow

Use this copy while iterating before publishing to GitHub.

```bash
npm install
npm run dev
```

Open the local Vite URL shown in the terminal, usually http://localhost:5173/.

When you are happy with the app, copy changes back into the GitHub-ready folder or publish this folder as the repo.
